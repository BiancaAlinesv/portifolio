services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bianca-portfolio
    ports:
      - "8003:80"
    env_file:
      - .env
    restart: unless-stopped
